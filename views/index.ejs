<!DOCTYPE html>
<html lang="en">
<% include ../partials/head %>
<body>
  <ul>
  <% for(var i = 0; i < data.length; i++) { %>
    <li>
        <%= data[i].lineage %>
    </li>
  <% } %>
  </ul>
</body>
</html>
